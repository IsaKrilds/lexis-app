import React from 'react';
import { Container } from './styles';
import StyledBanner from '../../components/StyledBanner';

const Home: React.FC = () => {
  return (
    <Container>
      <StyledBanner />
    </Container>
  );
};

export default Home;
